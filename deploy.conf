# ─────────────────────────────────────────────────────────────
# deploy.conf — Eluck Check-in Bot deployment identity
#
# This file defines which VPS directory and systemd services
# belong to THIS project (Eluck).  update.sh sources it.
# ─────────────────────────────────────────────────────────────

BOT_IDENTITY="Eluck Check-in Bot"
PROJECT=/opt/eluck-checkin-bot
VENV=$PROJECT/venv
BOT=eluck-checkin-bot
DASH=eluck-dashboard
